# 作文冒險 — 專案指南（給 Claude Code）

這是一個給高年級小學生的「遊戲化作文練習」網頁，目標是幫孩子準備**全民中文能力檢定（CWT）初等**寫作。

## 這是什麼

單一 HTML 檔的純前端網頁（無後端、無建置流程），用 RPG 遊戲的方式讓孩子練作文：
第一次開啟要先取一個練習生的名字，之後每題分「作戰計畫」→「謄寫完成」→「自我檢查」三關，做計畫時要審題、動手打段落大綱，系統依內容豐富度給浮動經驗值（XP），升等、解鎖徽章、點亮技能樹。同一題可以重練，分數取最高。進度存在瀏覽器的 localStorage。

## 檔案結構

- `index.html` — **整個 App 就是這一個檔案**（HTML + CSS + JS 全內嵌，無外部相依）。
- `manifest.webmanifest`、`icon-180/192/512.png` — 只為了「加到手機主畫面」用的圖示與設定。
  少了它們 App 照樣跑（直接開 `index.html` 只是沒圖示），所以「單一檔案可直接打開」的特性沒有破壞。
  圖示是墨藍底 + 金色「作」，要換的話三個尺寸都要一起換。
- `README.md` — 給人看的專案說明與部署方法。
- `CLAUDE.md` — 你正在讀的這份，開發時的規則與地圖。

## 技術重點

- 沒有框架、沒有 npm、沒有 build。直接用瀏覽器打開 `index.html` 就能跑。
- 所有狀態存在 `localStorage`，key 是 `writingQuestSave_v6`。
  **改動存檔資料結構時，一定要把 key 的版本號 +1**（例如 `_v7`），否則舊使用者載入舊資料會出錯。
  升版號時記得把舊 key 加進 `OLD_KEYS`，`load()` 會自動把舊存檔搬過來，孩子的等級不會被清掉。
- 樣式用 CSS 變數（`:root` 裡的 `--ink`/`--gold`/`--jade` 等），配色是「墨水靛藍 + 羊皮紙 + 經驗金」的冒險筆記風，改顏色從這裡改。

## 程式碼地圖（都在 index.html 的 `<script>` 裡）

- `QUESTS` 陣列 — **所有作文題目的資料**（目前 21 題）。加題目、改段落建議、改審題選項都動這裡（格式見下）。
  其中 15 題（`id` 以 `c_` 開頭）是 **CWT 全民中檢初等 2013–2024 歷屆真題**，引導語照官方「引導式作文」的格式改寫，`hint` 開頭會標年份。
  官方引導語固定三段：提問開場 → 舉三個例子擴大取材 → 明列必寫項目。**第三段就是 `options` 裡 `ok:true` 的來源**，寫新題目照這個邏輯拆最準。
- `TIERS` — 任務板的三個難度分區（入門／進階／挑戰），題目用 `tier:1/2/3` 對應。只是分區排列，不鎖關卡，孩子想寫哪題都可以。
- `analyze(text)` — **內容評分引擎**。依字數給基礎分，偵測到五感／對話／比喻／狀聲再加碼。規則刻意做得嚴一點，避免孩子用關鍵字騙分：
  - 詞庫：`SENSE_STRONG`（具體感覺，一個就算）／`SENSE_WEAK`（看、聽這類，要兩個不同的才算）／`SIMILE_STRONG`＋`SIMILE_PAT`＋`NOT_SIMILE`（`hasSimile()` 判斷，「好像／想像」不算比喻）／`SOUND`／`DIALOG_PAT`（引號裡至少兩個字）。
  - `squash()` 把連續重複的字或詞壓掉，灌水不算字數。
  - 少於 12 字不給技巧分；技巧同時計分的數量依字數設上限（18／28／40 字各多一項）。改分數平衡就動這裡。
- `LVL_XP` — 每個等級的門檻 XP。`RANKS` — 各等級的稱號。**兩個陣列長度必須一致**（`MAX_LV` 取 `RANKS.length`）。
  目前 10 級、頂標 3800 XP。21 題全部認真寫完大約 4500 XP，理論天花板約 7400 XP，所以最高等級拿得到但要玩到後期。加題目時記得回頭看這條曲線會不會太快封頂。
- `SKILLS` — 技能樹四項（對應中檢四大評分向度）。
- `BADGES` — 徽章清單與解鎖條件（條件邏輯在 `checkBadges()`）。
- `CHECKS` / `LADDER` — 攻略頁的自我檢查表與六級分階梯。
- 規劃關卡流程：`openPlan` → `drawPlan`（四步）→ `finalizePlan`（結算 XP、星級）→ `showResult`。
- `finishWrite` — 第二關「謄寫完成」領 XP，接著自動跳出這一篇的自我檢查。
- `redoPlan` — 同一題可以重練。`S.plans[qid]` 存 `xp`（歷來最高分）、`attempts`（挑戰次數），藍圖留分數最高那一版；**重做只給「比上次進步的差額」XP**，不能重複刷分。
- `openCheck(q)` — 第三關「自我檢查」，每一篇各做一次（`S.checks[qid]`），+20 XP、技能 s4 +1。攻略頁那份清單只是隨時可看的參考，不計分。
- 審題選項每次進入都用 `shuffledIdx()` 洗牌，順序存在 `PS.optOrder`（`QUESTS` 裡正解都寫在前面，靠這裡打亂）。
- 規劃中的內容會自動存成草稿 `S.draft`（`saveDraft`/`saveDraftSoon`/`clearDraft`），關掉再打開會接回來；規劃中點背景不會關閉視窗，只有「取消」會。
- `esc()` — 孩子打的字要進 HTML 一律先過這個，不然打到 `<` 會破版。
- `openNameGate(isRename)` — 開場的取名關卡。名字存在 `S.name`（最多 8 字），沒名字時啟動就會強制跳出來（`NAMEGATE=true`，這時點背景不會關）；hero 的「改名」按鈕帶 `true` 進來，可以取消。名字會出現在 hero、升級畫面和匯出的檔名。
- `planText(q)` / `copyPlan(q)` — 把藍圖整理成純文字、複製到剪貼簿（非 https 時自動退回 `execCommand` 的舊做法）。
- `exportSave()` / `importSave(file)` — 進度匯出成 JSON 檔、再匯回來。換手機或清快取靠這個救。

## 怎麼加一個新題目

在 `QUESTS` 陣列加一個物件，照這個格式：

```js
{id:"q7", tier:2, type:"記敘", title:"新題目名稱", writeXp:75, focus:"這題的重點",
 hint:"引導語：提問開場？舉兩三個例子？請寫出 A、B 和 C。",
 options:[            // 審題挑戰：ok:true=必寫，ok:false=偏題陷阱
   {t:"必寫重點一", ok:true},
   {t:"必寫重點二", ok:true},
   {t:"偏題陷阱一", ok:false},
   // ...建議 4 個必寫 + 3 個陷阱；正解寫在前面沒關係，畫面上每次都會重新洗牌
 ],
 segs:[              // 段落藍圖：四段，標 main 的是主場景，標 ending 的是結尾
   {t:"開頭", d:"這段要寫什麼的提示"},
   {t:"發展", d:"..."},
   {t:"主場景", d:"...", main:true},
   {t:"結尾", d:"...", ending:true}
 ]}
```

`type` 可用：記敘 / 抒情 / 想像 / 擬人 / 說明（顏色已定義好，對應 CSS 的 `.t-記敘` 等）。
`tier` 是難度分區：1 入門（好取材）、2 進階（要單點放大）、3 挑戰（抽象、論說、擬人）。
`writeXp` 照分區給：入門 60、進階 75、挑戰 90。
加完題目不用改別的地方，任務板會自動分到對應的區塊列出來。

寫真題的參考來源：官方歷屆佳作頁 <https://www.cwt.org.tw/cwtfrontend/HonorRoll.aspx>，
點進去每篇的第一段就是當年的引導語原文。

## 開發時的注意事項

- **改到存檔結構就升 `KEY` 版本號**（見上）。
- 改完在瀏覽器實際點一遍：做一次完整的計畫（四步）、看 XP 有沒有正確累加、升級動畫有沒有出來、重新整理頁面後進度還在不在。
- 這是給**兒童**用的，用字要簡單友善、鼓勵導向，不要出現負面或責備的字眼。
- **輸入框字級不能小於 16px**：iOS Safari 對更小的輸入框會自動放大整頁，孩子打字時畫面會抖。
- 版面有留 `env(safe-area-inset-*)`，改 `body`／`.wrap` 的 padding 時別把它拿掉，不然全螢幕開會被瀏海和 Home 條蓋到。
- 內容評分只是鼓勵孩子多寫、寫具體，不是真的批改。真正的作文批改仍需要老師/家長看稿紙。
- 保持「單一 HTML 檔可直接打開」這個特性，不要為了重構就引入框架或打包工具，除非有很強的理由。

## 部署（放上網路）

因為是純靜態網頁（整個資料夾一起上傳，圖示檔也要），最簡單的免費方式：
- **GitHub Pages**：把資料夾推到 GitHub repo，在 Settings → Pages 選 branch，網址就出來了。
- **Netlify / Cloudflare Pages / Vercel**：把資料夾拖進去或連 GitHub，自動部署。
- 手機用：部署後把網址存成手機主畫面捷徑，開起來就像 App。

詳細指令見 README.md。
